#include<stdio.h>

int main(){
	int num;
	printf("entre a number"); scanf("%d",&num);
	if (num % 2 == 0) printf("the %d is pair",num);
	else printf("the %d is impair",num);
	
	
	return 0;
	
}

